# Budget — the standing over-target report

Generated by `tools/doc-budget --write` (2026-07-25). **Regenerate it; don't hand-edit the sizes** — but the `answer`, `when` and `note` columns *are* hand-written, and every run carries them forward by path.

This file exists to **drain**. A row is a question, not a defect: answer it **split**, **trim** or **keep** and it leaves *Outstanding*. A recorded **keep** is permanent — the file is the right size for what it holds and the tool stops asking. ⭐ **A file that comes back over target after a `keep` is the signal worth having**: the process is failing to capture something, and that is a finding for the owner, not a licence to split.

**0 outstanding** · 2 answered with work pending · 1 settled *keep* · 0 shape findings.

## Outstanding — nobody has answered these yet

_Empty. That is the goal state._

## Answered — split or trim, work not done yet

| file | size | target | answer | when | note |
|---|---|---|---|---|---|
| `docs/ARCHITECTURE.md` | 268 lines | 150 | split | 2026-07-25 | Nine sections, and three of them are not architecture: *Automated checks*, *Playwright test conventions* and *Running checks locally* are gate and test material that **already has homes** in `GATES.md` and `TESTING.md`. The seam is clean — structure, import rules and where a file goes stay; how CI checks it leaves. ⚠️ This is a de-duplication, not a move: the overlapping text has to be reconciled rather than relocated. |
| `docs/VOICE.md` | 241 lines | 150 | split | 2026-07-25 | Two reading occasions in one file. Roughly 200 lines describe the **system** — SpeechService, the speakable helpers, the CSS contract, the speak-after-action ordering rule — and are read once. The last three sections (*Adding a new speakable element*, *Migrating an existing activity*, *Tests*) are read **every time** someone touches a speakable element. Split on that line: `VOICE.md` keeps the system, a `VOICE-CHECKLIST.md` carries the task-time half. |

## Settled — `keep`, and the tool no longer asks

Each was looked at and judged the right size for what it holds.

| file | size | target | answer | when | note |
|---|---|---|---|---|---|
| `docs/MODULES.md` | 131 lines | 60 | keep | 2026-07-25 | It is a table of every `core/`, `ui/`, `components/` and `content/` module against its purpose — an index that is exactly as long as the module count, with nothing to split on and no prose to trim. The entry already marks it ⛔ *don't keep it resident*, which is the real cost control. Same heuristic class as `product-homeschooling/docs/PHONICS.md`. |

## Shape findings — index or doc, not both

`MACHINERY-SHAPE.md` §1: an **index** points and never explains; a **doc** explains one concern and never points onward. A file whose position says one and whose content says the other is a defect independent of its length.

_None._

**Refs:** `MACHINERY-SHAPE.md` · `tools/doc-budget.md`
